<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Future of Web Development in 2023 | NexusBlog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #4e54c8;
            --main-color: #1F262D;
            --primary-gradient: linear-gradient(135deg, #1F262D 0%, #3A4450 100%);
            --primary-hover: #2A323B;
            --primary-active: #14181C;
            --primary-text: #ffffff;
            --primary-light: #3A4450;
            --primary-lighter: #525E6E;
            --secondary-color: #6c757d;
            --accent-color: #ff6b6b;
            --light-bg: #f8f9fa;
            --dark-text: #2d3436;
            --light-text: #636e72;
            --card-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            --card-hover-shadow: 0 15px 45px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s ease;
            --border-radius: 12px;
            --edit-color: #ffa726;
            --edit-light: #ffb74d;
            --delete-color: #ef5350;
            --delete-light: #e57373;
            --card-bg: #ffffff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--dark-text);
            background-color: #f9fafb;
            padding-top: 80px;
            line-height: 1.7;
        }

        .navbar {
            background: var(--primary-gradient);
            box-shadow: 0 4px 18px rgba(0, 0, 0, 0.1);
            padding: 12px 0;
            transition: var(--transition);
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.8rem;
            color: var(--primary-text) !important;
            display: flex;
            align-items: center;
        }

        .navbar-brand span {
            color: var(--accent-color);
        }

        .nav-link {
            color: rgba(255, 255, 255, 0.85) !important;
            font-weight: 500;
            margin: 0 8px;
            transition: var(--transition);
            position: relative;
        }

        .nav-link:hover {
            color: var(--primary-text) !important;
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent-color);
            transition: var(--transition);
        }

        .navbar-toggler {
            border: none;
            color: var(--primary-text);
        }

        .search-form {
            position: relative;
            margin-right: 15px;
        }

        .search-input {
            background: var(--primary-light);
            border: none;
            border-radius: 50px;
            color: white;
            padding: 10px 20px;
            padding-right: 45px;
            width: 280px;
            transition: var(--transition);
        }

        .search-input:focus {
            outline: none;
            box-shadow: 0 0 0 2px var(--accent-color);
            background: var(--primary-lighter);
        }

        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .search-btn {
            position: absolute;
            right: 5px;
            top: 50%;
            transform: translateY(-50%);
            background: transparent;
            border: none;
            color: rgba(255, 255, 255, 0.7);
            cursor: pointer;
        }

        .btn-signup {
            background: var(--accent-color);
            color: white;
            border-radius: 50px;
            padding: 8px 20px;
            font-weight: 600;
            transition: var(--transition);
        }

        .btn-signup:hover {
            background: #ff5252;
            transform: translateY(-2px);
        }

        /* New Blog Layout Styles */
        .blog-container {
            max-width: 900px;
            margin: 0 auto;
            padding: 0 15px;
        }

        .blog-header {
            margin-bottom: 40px;
            text-align: center;
            position: relative;
        }

        .blog-title {
            font-size: 2.8rem;
            font-weight: 800;
            color: var(--main-color);
            margin-bottom: 20px;
            line-height: 1.3;
            position: relative;
            padding-bottom: 20px;
        }

        

        .blog-meta {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 30px;
            gap: 15px;
        }

        .author-img {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid white;
            box-shadow: var(--card-shadow);
        }

        .meta-text {
            text-align: left;
        }

        .author-name {
            font-weight: 700;
            margin-bottom: 3px;
            color: var(--main-color);
        }

        .publish-date {
            color: var(--light-text);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .publish-date i {
            font-size: 0.8rem;
        }

        .read-time {
            background: var(--light-bg);
            padding: 8px 16px;
            border-radius: 50px;
            font-size: 0.9rem;
            color: var(--light-text);
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .blog-hero-img {
            width: 100%;
            border-radius: var(--border-radius);
            margin-bottom: 40px;
            box-shadow: var(--card-shadow);
            transition: var(--transition);
        }

        .blog-hero-img:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-hover-shadow);
        }

        .blog-content {
            font-size: 1.1rem;
            color: var(--dark-text);
            position: relative;
        }

        .blog-content p {
            margin-bottom: 25px;
        }

        .blog-content h2 {
            font-weight: 700;
            margin: 50px 0 25px;
            color: var(--main-color);
            position: relative;
            padding-left: 20px;
        }

        .blog-content h2::before {
            content: '';
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 8px;
            height: 8px;
            background: var(--primary-color);
            border-radius: 50%;
        }

        .blog-content h3 {
            font-weight: 600;
            margin: 40px 0 20px;
            color: var(--main-color);
            padding-left: 15px;
            border-left: 4px solid var(--primary-color);
        }

        .blog-content blockquote {
            border-left: 4px solid var(--primary-color);
            padding: 25px 20px 25px 30px;
            margin: 40px 0;
            font-style: italic;
            color: var(--light-text);
            background: var(--light-bg);
            border-radius: 0 var(--border-radius) var(--border-radius) 0;
            position: relative;
        }

        .blog-content blockquote::before {
            content: '\201C';
            position: absolute;
            top: 10px;
            left: 10px;
            font-size: 3rem;
            color: var(--primary-color);
            opacity: 0.2;
        }

        .blog-content img {
            width: 100%;
            border-radius: var(--border-radius);
            margin: 30px 0;
            box-shadow: var(--card-shadow);
        }

        .tag-list {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin: 40px 0;
            justify-content: center;
        }

        .tag {
            background: var(--light-bg);
            color: var(--dark-text);
            padding: 8px 18px;
            border-radius: 50px;
            font-size: 0.9rem;
            text-decoration: none;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .tag:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-2px);
        }

        /* Social Sharing */
        .social-sharing {
            display: flex;
            gap: 15px;
            margin: 40px 0;
            justify-content: center;
        }

        .social-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: var(--light-bg);
            color: var(--dark-text);
            text-decoration: none;
            transition: var(--transition);
        }

        .social-btn:hover {
            background: var(--primary-color);
            color: white;
            transform: translateY(-3px);
        }

        /* Author Bio - Improved */
        .author-bio {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 30px;
            margin: 60px 0;
            box-shadow: var(--card-shadow);
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
        }

        .author-bio::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 5px;
            height: 100%;
            background: var(--primary-color);
        }

        .author-bio-img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 25px;
            border: 3px solid white;
            box-shadow: var(--card-shadow);
        }

        .author-bio-text h4 {
            font-weight: 700;
            margin-bottom: 10px;
            color: var(--main-color);
        }

        .author-bio-text p {
            margin-bottom: 0;
            color: var(--light-text);
        }

        /* Related Posts - Improved */
        .related-posts {
            margin: 60px 0;
        }

        .section-title {
            font-weight: 700;
            color: var(--main-color);
            margin-bottom: 30px;
            position: relative;
            padding-bottom: 15px;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 100%;
            height: 3px;
            background: var(--primary-color);
        }

        .related-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--card-shadow);
            transition: var(--transition);
            height: 100%;
            border: none;
            position: relative;
        }

        .related-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-hover-shadow);
        }

        .related-card-img {
            height: 180px;
            object-fit: cover;
            transition: var(--transition);
        }

        .related-card:hover .related-card-img {
            transform: scale(1.05);
        }

        .related-card-body {
            padding: 20px;
        }

        .related-card-title {
            font-weight: 700;
            color: var(--main-color);
            margin-bottom: 15px;
            font-size: 1.2rem;
            line-height: 1.4;
        }

        .related-card-date {
            color: var(--light-text);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        /* Comments Section - Improved */
        .comments-section {
            margin: 60px 0;
        }

        .comment-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
        }

        .comment-count {
            background: var(--primary-color);
            color: white;
            width: 35px;
            height: 35px;
            border-radius: 50%;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            margin-left: 10px;
        }

        .comment {
            display: flex;
            margin-bottom: 30px;
            position: relative;
        }

        .comment::before {
            content: '';
            position: absolute;
            top: 70px;
            left: 30px;
            bottom: 0;
            width: 2px;
            background: var(--light-bg);
        }

        .comment-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            margin-right: 20px;
            border: 3px solid white;
            box-shadow: var(--card-shadow);
        }

        .comment-body {
            flex: 1;
            background: var(--light-bg);
            padding: 20px;
            border-radius: var(--border-radius);
            position: relative;
        }

        .comment-body::before {
            content: '';
            position: absolute;
            top: 20px;
            left: -10px;
            width: 0;
            height: 0;
            border-top: 10px solid transparent;
            border-bottom: 10px solid transparent;
            border-right: 10px solid var(--light-bg);
        }

        .comment-author {
            font-weight: 700;
            margin-bottom: 5px;
            color: var(--main-color);
        }

        .comment-date {
            color: var(--light-text);
            font-size: 0.9rem;
            margin-bottom: 15px;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .comment-reply {
            color: var(--primary-color);
            font-weight: 600;
            text-decoration: none;
            font-size: 0.9rem;
            display: inline-flex;
            align-items: center;
            gap: 5px;
            transition: var(--transition);
        }

        .comment-reply:hover {
            color: var(--accent-color);
        }

        /* Comment Form - Simplified */
        .comment-form {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 30px;
            box-shadow: var(--card-shadow);
            margin-top: 40px;
            position: relative;
        }

        .comment-form::before {
            content: '';
            position: absolute;
            top: -10px;
            left: 30px;
            width: 0;
            height: 0;
            border-left: 10px solid transparent;
            border-right: 10px solid transparent;
            border-bottom: 10px solid var(--card-bg);
        }

        .form-title {
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--main-color);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .form-title i {
            color: var(--primary-color);
        }

        .form-control {
            padding: 15px;
            border: 1px solid rgba(0, 0, 0, 0.1);
            border-radius: var(--border-radius);
            margin-bottom: 20px;
            transition: var(--transition);
            font-size: 1rem;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(78, 84, 200, 0.1);
        }

        .btn-primary {
            background: var(--primary-color);
            border: none;
            padding: 12px 30px;
            font-weight: 600;
            transition: var(--transition);
            border-radius: 50px;
        }

        .btn-primary:hover {
            background: var(--primary-light);
            transform: translateY(-2px);
        }

        /* Footer */
        .footer {
            background: var(--main-color);
            color: white;
            padding: 60px 0 30px;
            margin-top: 60px;
        }

        .footer-title {
            font-weight: 700;
            margin-bottom: 25px;
            font-size: 1.4rem;
        }

        .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--accent-color);
            padding-left: 5px;
        }

        .social-links {
            display: flex;
            gap: 15px;
        }

        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary-light);
            color: white;
            text-decoration: none;
            transition: var(--transition);
        }

        .social-links a:hover {
            background: var(--accent-color);
            transform: translateY(-3px);
        }

        .copyright {
            text-align: center;
            padding-top: 30px;
            margin-top: 30px;
            border-top: 1px solid var(--primary-light);
            color: rgba(255, 255, 255, 0.6);
        }

        /* Responsive Adjustments */
        @media (max-width: 992px) {
            .search-input {
                width: 200px;
            }
            
            .blog-title {
                font-size: 2.3rem;
            }
        }

        @media (max-width: 768px) {
            .search-form {
                margin: 15px 0;
                width: 100%;
            }
            
            .search-input {
                width: 100%;
            }
            
            .blog-title {
                font-size: 2rem;
            }
            
            .author-bio {
                flex-direction: column;
                text-align: center;
            }
            
            .author-bio-img {
                margin-right: 0;
                margin-bottom: 20px;
            }
            
            .comment {
                flex-direction: column;
            }
            
            .comment-avatar {
                margin-bottom: 15px;
            }
            
            .comment-body::before {
                display: none;
            }
        }

        .category-btn{
              padding: 10px 14px;
             background-color: var(--primary-color);
             border-radius: 30px;
             color:  white;
             font-size: 14px;

        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
          <a class="navbar-brand" href="#">Nexus<span>Blog</span></a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarContent">
            <ul class="navbar-nav me-auto">
              <li class="nav-item"><a class="nav-link" href="/webPage/home">Home</a></li>
              <li class="nav-item"><a class="nav-link" href="#">Categories</a></li>
              <li class="nav-item"><a class="nav-link" href="#">Trending</a></li>
              <li class="nav-item"><a class="nav-link" href="#">About</a></li>
              <li class="nav-item"><a class="nav-link" href="#">Contact</a></li>
            </ul>

            <form class="search-form d-none d-lg-block">
                <input type="text" class="search-input" placeholder="Search articles...">
                <button type="submit" class="search-btn">
                    <i class="bi bi-search"></i>
                </button>
            </form>

            <div class="d-flex">
                <% if (session.user) { %>
                  <span class="me-3 btn btn-signup "><%= session.user.firstName %> <%= session.user.lastName %></span>
                  <a href="/webPage/logoutUser" class="btn btn-signup">Logout</a>
                <% } else { %>
                  <a href="/webPage/userLogin" class="btn btn-signup me-2">Login</a>
                <% } %>
              </div>
            
          </div>
        </div>
      </nav>
    
      <div class="container blog-container">
        <h1 class="blog-title"><%= blog.title %></h1>
        <div class="d-flex justify-content-between">

            <div class="d-flex align-items-center mb-3">
                <img src="<%= blog.authImage %>" alt="Author" class="author-img me-2">
                <div>
                    <div><strong><%= blog.authName %></strong></div>
                    <small class="text-muted"> publish-date : <i class="bi bi-calendar"></i> <%= new Date(blog.pubDate).toLocaleDateString() %></small>
                </div>
            </div>
            <div ><strong class="category-btn"><%= blog.category %></strong></div>
        </div>
      
        <% if (blog.image) { %>
          <img src="<%= blog.image %>" alt="<%= blog.title %>" class="blog-hero-img">
        <% } %>
    
        <div class="blog-content">
          <%- blog.desc || blog.content %>
        </div>
    
        <div class="comments-section">
          <h3>Comments (<%= blog.comments ? blog.comments.length : 0 %>)</h3>
    
          <% if (blog.comments && blog.comments.length > 0) { %>
            <% blog.comments.forEach(c => { %>
              <div class="comment">
                <img src="https://ui-avatars.com/api/?name=<%= c.name %>&background=random" alt="<%= c.name %>" class="comment-avatar">
                <div class="comment-body">
                  <div class="comment-author"><%= c.name %></div>
                  <div class="comment-date"><i class="bi bi-clock"></i> <%= new Date(c.createdAt).toLocaleString() %></div>
                  <p><%= c.text %></p>
                </div>
              </div>
            <% }) %>
          <% } else { %>
            <p class="text-muted">No comments yet. Be the first to comment!</p>
          <% } %>
        </div>
    
        <div class="comment-form mt-4">
          <h4><i class="bi bi-chat-dots"></i> Leave a Comment</h4>
          <form action="/webPage/home/single-blog/<%= blog._id %>/comment" method="POST">
            <textarea name="text" class="form-control mb-2" rows="4" placeholder="Your Comment" required></textarea>
            <button type="submit" class="btn btn-primary">Post Comment</button>
          </form>
        </div>
      </div>
    
      <footer class="footer mt-5 py-4 text-center bg-dark text-light">
        <p class="mb-0">&copy; <%= new Date().getFullYear() %> NexusBlog. All rights reserved.</p>
      </footer>
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>