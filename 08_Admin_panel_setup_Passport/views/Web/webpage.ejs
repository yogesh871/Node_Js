<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NexusBlog | Premium Content Hub</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #4e54c8;
            --main-color: #1F262D;
            --primary-gradient: linear-gradient(135deg, #1F262D 0%, #3A4450 100%);
            --primary-hover: #2A323B;
            --primary-active: #14181C;
            --primary-text: #ffffff;
            --primary-light: #3A4450;
            --primary-lighter: #525E6E;
            --secondary-color: #6c757d;
            --accent-color: #ff6b6b;
            --light-bg: #f8f9fa;
            --dark-text: #2d3436;
            --light-text: #636e72;
            --card-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            --card-hover-shadow: 0 15px 45px rgba(0, 0, 0, 0.12);
            --transition: all 0.3s ease;
            --border-radius: 12px;
            --edit-color: #ffa726;
            --edit-light: #ffb74d;
            --delete-color: #ef5350;
            --delete-light: #e57373;
            --card-bg: #ffffff;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: var(--dark-text);
            background-color: #f9fafb;
            padding-top: 80px;
        }

        /* Header Styles */
        .navbar {
            background: var(--primary-gradient);
            box-shadow: 0 4px 18px rgba(0, 0, 0, 0.1);
            padding: 12px 0;
            transition: var(--transition);
        }

        .navbar-brand {
            font-weight: 700;
            font-size: 1.8rem;
            color: var(--primary-text) !important;
            display: flex;
            align-items: center;
        }

        .navbar-brand span {
            color: var(--accent-color);
        }

        .nav-link {
            color: rgba(255, 255, 255, 0.85) !important;
            font-weight: 500;
            margin: 0 8px;
            transition: var(--transition);
            position: relative;
        }

        .nav-link:hover {
            color: var(--primary-text) !important;
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--accent-color);
            transition: var(--transition);
        }

        .navbar-toggler {
            border: none;
            color: var(--primary-text);
        }

        .search-form {
            position: relative;
            margin-right: 15px;
        }

        .search-input {
            background: var(--primary-light);
            border: none;
            border-radius: 50px;
            color: white;
            padding: 10px 20px;
            padding-right: 45px;
            width: 280px;
            transition: var(--transition);
        }

        .search-input:focus {
            outline: none;
            box-shadow: 0 0 0 2px var(--accent-color);
            background: var(--primary-lighter);
        }

        .search-input::placeholder {
            color: rgba(255, 255, 255, 0.7);
        }

        .search-btn {
            position: absolute;
            right: 5px;
            top: 50%;
            transform: translateY(-50%);
            background: transparent;
            border: none;
            color: rgba(255, 255, 255, 0.7);
            cursor: pointer;
        }

        .btn-signup {
            background: var(--accent-color);
            color: white;
            border-radius: 50px;
            padding: 8px 20px;
            font-weight: 600;
            transition: var(--transition);
        }

        .btn-signup:hover {
            background: #ff5252;
            transform: translateY(-2px);
        }

        /* Hero Section */
        .hero-section {
            background: var(--primary-gradient);
            color: white;
            padding: 80px 0;
            border-radius: 0 0 var(--border-radius) var(--border-radius);
            margin-bottom: 40px;
        }

        .hero-content {
            max-width: 650px;
        }

        .hero-title {
            font-size: 3.2rem;
            font-weight: 800;
            margin-bottom: 20px;
        }

        .hero-desc {
            font-size: 1.2rem;
            margin-bottom: 30px;
            opacity: 0.9;
        }

        .hero-btn {
            background: var(--accent-color);
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
            font-weight: 600;
            font-size: 1.1rem;
            transition: var(--transition);
        }

        .hero-btn:hover {
            background: #ff5252;
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
        }

        /* Blog Cards */
        .blog-card {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            overflow: hidden;
            box-shadow: var(--card-shadow);
            transition: var(--transition);
            height: 100%;
            border: none;
        }

        .blog-card:hover {
            transform: translateY(-5px);
            box-shadow: var(--card-hover-shadow);
        }

        .card-img-top {
            height: 200px;
            object-fit: cover;
        }

        .card-category {
            background: var(--main-color);
            color: white;
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 0.8rem;
            font-weight: 600;
            position: absolute;
            top: 15px;
            left: 15px;
        }

        .card-title {
            font-weight: 700;
            color: var(--main-color);
            margin-bottom: 15px;
        }

        .card-text {
            color: var(--light-text);
            margin-bottom: 20px;
        }

        .card-footer {
            background: transparent;
            border-top: 1px solid rgba(0, 0, 0, 0.05);
        }

        .author-img {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            object-fit: cover;
        }

        .read-more {
            color: var(--main-color);
            font-weight: 600;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            transition: var(--transition);
        }

        .read-more:hover {
            color: var(--accent-color);
        }

        .read-more i {
            margin-left: 5px;
            transition: var(--transition);
        }

        .read-more:hover i {
            transform: translateX(3px);
        }

        /* Sidebar */
        .sidebar-widget {
            background: var(--card-bg);
            border-radius: var(--border-radius);
            padding: 25px;
            margin-bottom: 30px;
            box-shadow: var(--card-shadow);
        }

        .widget-title {
            font-weight: 700;
            color: var(--main-color);
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 2px solid rgba(0, 0, 0, 0.05);
        }

        .category-list {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .category-list li {
            margin-bottom: 12px;
            display: flex;
            justify-content: space-between;
        }

        .category-list a {
            color: var(--dark-text);
            text-decoration: none;
            transition: var(--transition);
        }

        .category-list a:hover {
            color: var(--main-color);
        }

        .category-list span {
            background: var(--light-bg);
            padding: 2px 10px;
            border-radius: 50px;
            font-size: 0.8rem;
        }

        .tags-cloud {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
        }

        .tag {
            background: var(--light-bg);
            color: var(--dark-text);
            padding: 5px 15px;
            border-radius: 50px;
            font-size: 0.9rem;
            text-decoration: none;
            transition: var(--transition);
        }

        .tag:hover {
            background: var(--main-color);
            color: white;
        }

        /* Footer */
        .footer {
            background: var(--main-color);
            color: white;
            padding: 60px 0 30px;
            margin-top: 60px;
        }

        .footer-title {
            font-weight: 700;
            margin-bottom: 25px;
            font-size: 1.4rem;
        }

        .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: var(--transition);
        }

        .footer-links a:hover {
            color: var(--accent-color);
            padding-left: 5px;
        }

        .social-links {
            display: flex;
            gap: 15px;
        }

        .social-links a {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--primary-light);
            color: white;
            text-decoration: none;
            transition: var(--transition);
        }

        .social-links a:hover {
            background: var(--accent-color);
            transform: translateY(-3px);
        }

        .copyright {
            text-align: center;
            padding-top: 30px;
            margin-top: 30px;
            border-top: 1px solid var(--primary-light);
            color: rgba(255, 255, 255, 0.6);
        }

        /* Responsive Adjustments */
        @media (max-width: 992px) {
            .search-input {
                width: 200px;
            }
            
            .hero-title {
                font-size: 2.5rem;
            }
        }

        @media (max-width: 768px) {
            .search-form {
                margin: 15px 0;
                width: 100%;
            }
            
            .search-input {
                width: 100%;
            }
            
            .hero-title {
                font-size: 2.2rem;
            }
        }

        .main-color{
            background-color: var(--main-color);
            color:  white;
        }

        .main-color:hover {
           background-color: var(--primary-hover);
           color: white;
        }

 .page-link{
     color:var(--main-color);
 }
 .page-link :hover{
     color: white;
     background-color: var(--main-color);
 }
 .active>.page-link{
    background-color: var(--main-color);
    color:white;
 }
 .active:hover{
    border-color: var(--main-color);
    color: var(--main-color);
 }.sub-btn{
    background-color: var(--accent-color);
    color: white;
 }
 .sub-btn:hover{
    background-color: var(--delete-light);
    color: white;
 }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top">
        <div class="container">
            <a class="navbar-brand" href="#">Nexus<span>Blog</span></a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" href="/webPage/home">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Categories</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Trending</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Contact</a>
                    </li>
                </ul>
                
                <form class="search-form d-none d-lg-block">
                    <input type="text" class="search-input" placeholder="Search articles...">
                    <button type="submit" class="search-btn">
                        <i class="bi bi-search"></i>
                    </button>
                </form>
                
                

                <div class="d-flex">
                    <% if (session.user) { %>
                      <span class="me-3 btn btn-signup "><%= session.user.firstName %> <%= session.user.lastName %></span>
                      <a href="/webPage/logoutUser" class="btn btn-signup">Logout</a>
                    <% } else { %>
                      <a href="/webPage/userLogin" class="btn btn-signup me-2">Login</a>
                    <% } %>
                  </div>
                  
            </div>
        </div>
    </nav>

    <section class="hero-section">
        <div class="container">
            <div class="hero-content">
                <h1 class="hero-title">Discover Stories, Ideas, and Expertise</h1>
                <p class="hero-desc">Explore a world of knowledge with expertly crafted articles on technology, design, business, and more from creative minds around the globe.</p>
                <button class="hero-btn">Start Reading</button>
            </div>
        </div>
    </section>

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h2 class="mb-4">Featured Articles</h2>
              
                <div class="row">
                  <% blogs.forEach(blog => { %>
                    <div class="col-md-4 mb-4">
                      <div class="blog-card card h-100 shadow-sm">
                        <div class="position-relative">
                          <img src="<%= blog.image %>" class="card-img-top" alt="Blog Image" style="height:220px;object-fit:cover;">
                          <div class="card-category badge main-color position-absolute top-0 start-0 m-2 px-3 ">
                            <%= blog.category %>
                          </div>
                        </div>
              
                        <div class="card-body d-flex flex-column">
                          <h5 class="card-title"> <%= blog.title.split(" ").slice(0,5).join(" ") %>
                            <% if (blog.title.split(" ").length > 10) { %>...<% } %>
                        </h5>
              
                          <p class="card-text">
                            <%= blog.desc.split(" ").slice(0,15).join(" ") %>
                           
                            
                          </p>
              
            
              
                          <div class="d-flex align-items-center mt-auto">
                            <img src="<%= blog.authImage %>" alt="Author" class="author-img rounded-circle me-3" style="width:40px;height:40px;object-fit:cover;">
                            <div>
                              <h6 class="mb-0"><%= blog.authName %></h6>
                              <small class="text-muted"><%= new Date(blog.pubDate).toLocaleDateString() %></small>
                            </div>
                          </div>
                        </div>
              
                        <div class="card-footer bg-transparent border-0 text-end">
                          <a href="/webPage/home/single-blog/<%= blog._id %>" class="btn main-color btn-sm">Read More</a>
                        </div>
                      </div>
                    </div>
                  <% }) %>
                </div>
              
                <nav aria-label="Blog pagination">
                  <ul class="pagination justify-content-center">
                    <% if (currentPage > 1) { %>
                      <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage - 1 %>">Previous</a>
                      </li>
                    <% } else { %>
                      <li class="page-item disabled"><a class="page-link">Previous</a></li>
                    <% } %>
              
                    <% for (let i = 1; i <= totalPages; i++) { %>
                      <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                        <a class="page-link" href="?page=<%= i %>"><%= i %></a>
                      </li>
                    <% } %>
              
                    <% if (currentPage < totalPages) { %>
                      <li class="page-item">
                        <a class="page-link" href="?page=<%= currentPage + 1 %>">Next</a>
                      </li>
                    <% } else { %>
                      <li class="page-item disabled"><a class="page-link">Next</a></li>
                    <% } %>
                  </ul>
                </nav>
              </div>
              
            
           
        </div>
    </div>

    <footer class="footer">
        <div class="container">
            <div class="row">
                <div class="col-md-4 mb-4 mb-md-0">
                    <h5 class="footer-title">NexusBlog</h5>
                    <p>A platform for sharing knowledge, ideas, and experiences. Join our community of readers and writers today.</p>
                    <div class="social-links">
                        <a href="#"><i class="bi bi-facebook"></i></a>
                        <a href="#"><i class="bi bi-twitter"></i></a>
                        <a href="#"><i class="bi bi-instagram"></i></a>
                        <a href="#"><i class="bi bi-linkedin"></i></a>
                    </div>
                </div>
                
                <div class="col-md-2 mb-4 mb-md-0">
                    <h5 class="footer-title">Quick Links</h5>
                    <ul class="footer-links">
                        <li><a href="#">Home</a></li>
                        <li><a href="#">About</a></li>
                        <li><a href="#">Categories</a></li>
                        <li><a href="#">Trending</a></li>
                        <li><a href="#">Contact</a></li>
                    </ul>
                </div>
                
                <div class="col-md-2 mb-4 mb-md-0">
                    <h5 class="footer-title">Categories</h5>
                    <ul class="footer-links">
                        <li><a href="#">Technology</a></li>
                        <li><a href="#">Business</a></li>
                        <li><a href="#">Design</a></li>
                        <li><a href="#">Health</a></li>
                        <li><a href="#">Travel</a></li>
                    </ul>
                </div>
                
                <div class="col-md-4">
                    <h5 class="footer-title">Newsletter</h5>
                    <p>Subscribe to our newsletter to get updates on our latest articles.</p>
                    <form>
                        <div class="input-group mb-3">
                            <input type="email" class="form-control" placeholder="Your email address">
                            <button class="btn sub-btn" type="button">Subscribe</button>
                        </div>
                    </form>
                </div>
            </div>
            
            <div class="copyright">
                <p>© 2023 NexusBlog. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>